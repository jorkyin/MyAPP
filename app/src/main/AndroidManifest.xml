<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jorkyin.myapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".listViewDemo.ListViewDemo"></activity>
        <activity android:name=".girdviewdemo.GridViewDemo"></activity>
        <activity android:name=".scrolDemo.ScrolViewDemo"></activity>
        <activity android:name=".servers.MusicActivity"></activity>
        <activity android:name=".myview.TestRedButtonActivity"></activity>
        <activity android:name=".myFragment.TestFragmentActivity"></activity>

        <service android:name=".servers.MusicService"></service>

    </application>

</manifest>
